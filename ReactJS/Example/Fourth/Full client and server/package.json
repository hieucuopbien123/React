{
  "name":"express",
  "version":"1.0.0",
  "description":"this is description of app",
  "dependencies": {
    "concurrently": "^6.2.0",
    "express": "^4.17.1",
    "mssql": "^7.2.0"
  },
  "scripts": {
    "server": "node app.js",
    "client": "npm run start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
